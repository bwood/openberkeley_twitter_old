<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function openberkeley_twitter_ctools_content_type_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools' && !empty($plugin)) {
    return "plugins/$plugin";
  }
}

